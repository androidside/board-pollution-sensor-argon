# set(COMPONENT_SRCDIRS "." )
# set(COMPONENT_ADD_INCLUDEDIRS ".")

# # set(COMPONENT_SRCS "main.c" "fetch.c" "connect.c")
# set(COMPONENT_ADD_INCLUDEDIRS "." "argon_reading/.")
#set(COMPONENT_SRCDIRS ".")
# set(COMPONENT_SRCS "main.c" "fetch.c" "connect.c")
#set(COMPONENT_SRCS ".")
#set(COMPONENT_ADD_INCLUDEDIRS ".")

set(COMPONENT_SRCDIRS ".")
# set(COMPONENT_SRCS "main.c" "fetch.c" "connect.c")
set(COMPONENT_ADD_INCLUDEDIRS ".")

register_component()
